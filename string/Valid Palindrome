  bool check(string &s , int start ,int end )
    {  
        if(start>=end)
        return 1;

        if(s[start]!=s[end]) return 0;
      
       return check(s , start+1 , end-1);
    }
    bool isPalindrome(string s) {
         string result;

    // Filter: keep only alphanumeric characters, convert to lowercase
    for (char c : s) {
        if (isalnum(c)) {
            result += tolower(c);
        }
    }

    // Check if the cleaned string is a palindrome using recursion
    return check(result, 0, result.size() - 1); 
    } 

t.c== o(n)
s.c==o(n)
