   int getCount(Node* head) {
        // Code here
        int count=0 ; 
        
        Node*temp=head;
        
        while( temp)
        { 
            count++;
            temp=temp->next;
        }
        
        return count;
    }

t.c= o(n)
s.c=o(1)
